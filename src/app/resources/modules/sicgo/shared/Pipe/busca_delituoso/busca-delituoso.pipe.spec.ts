import { BuscaDelituosoPipe } from './busca-delituoso.pipe';

describe('BuscaDelituosoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaDelituosoPipe();
    expect(pipe).toBeTruthy();
  });
});
