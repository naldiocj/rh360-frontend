import { BuscaGruposAssociadosPipe } from './busca-grupos-associados.pipe';

describe('BuscaGruposAssociadosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaGruposAssociadosPipe();
    expect(pipe).toBeTruthy();
  });
});
