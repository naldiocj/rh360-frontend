import { BuscaGrupoPipe } from './busca-grupo.pipe';

describe('BuscaGrupoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaGrupoPipe();
    expect(pipe).toBeTruthy();
  });
});
